# Optional: enables the AI formula assistant to use Claude instead of the
# built-in keyword-based fallback. Without this set, /api/ai/formula still
# works using simple keyword matching.
ANTHROPIC_API_KEY=

# Set to 1 when deploying a public demo. Turns the live-data sources feature off
# on both the API and the UI — see SECURITY.md for why that matters, and
# src/lib/demoMode.ts for what it does.
NEXT_PUBLIC_DEMO_MODE=

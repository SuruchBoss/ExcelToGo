# ExcelToGo

เว็บแอปที่แปลงตาราง Excel ให้กลายเป็น UI ที่กรอกง่ายสำหรับคนทั่วไป มีสูตรสำเร็จรูปให้ลากวางแทนการจำสูตรเอง
มีผู้ช่วย AI ที่แนะนำสูตรจากคำอธิบายภาษาไทย/อังกฤษ รองรับหลายชีตในไฟล์เดียว คัดลอก/วาง จัดรูปแบบเซลล์
เรียงลำดับและกรองข้อมูล บันทึกอัตโนมัติพร้อม Undo/Redo และส่งออกกลับเป็นไฟล์ Excel (ครบทุกชีต) หรือ PDF ได้ทันที

สร้างด้วย **Next.js (App Router) + TypeScript** ตามที่ต้องการ รันได้ทั้งในเครื่องตัวเองและ deploy ขึ้นเว็บได้ฟรีผ่าน Vercel

![หน้าจอหลักของ ExcelToGo: ตารางกรอกข้อมูลพร้อมแถบสูตรด้านขวา](docs/screenshots/01-overview.png)

---

## สารบัญ

1. [Tech Stack](#tech-stack)
2. [วิธีดึงไปใช้งาน](#วิธีดึงไปใช้งาน)
3. [ฟีเจอร์และวิธีใช้อย่างละเอียด](#ฟีเจอร์และวิธีใช้อย่างละเอียด)
4. [โครงสร้างโปรเจกต์](#โครงสร้างโปรเจกต์)
5. [รายการสูตรที่รองรับ](#รายการสูตรที่รองรับ)
6. [ปัญหาที่พบบ่อย](#ปัญหาที่พบบ่อย)

---

## Tech Stack

### Framework / ภาษา
| เทคโนโลยี | เวอร์ชัน | ใช้ทำอะไร |
|---|---|---|
| [Next.js](https://nextjs.org) | 16 (App Router) | เฟรมเวิร์กหลักของเว็บแอป ทั้งหน้าเว็บ (client component) และ API route (`/api/ai/formula`) |
| [React](https://react.dev) | 19 | UI library |
| [TypeScript](https://www.typescriptlang.org) | 5 | type safety ทั้งโปรเจกต์ รวมถึงเอนจินคำนวณสูตรที่เขียนเอง |
| [Tailwind CSS](https://tailwindcss.com) | 4 | จัดสไตล์ทั้งหมดแบบ utility-class |

### ไลบรารีหลักที่ใช้
| ไลบรารี | ใช้ทำอะไร |
|---|---|
| `zustand` | จัดการ state ส่วนกลางของแอป (ตาราง, selection, แผงสูตรที่กำลังกรอก, แถบข้างที่เปิดอยู่) ใน `src/store/sheetStore.ts` แทนการส่ง props เป็นทอดๆ พร้อม middleware `persist` สำหรับบันทึกอัตโนมัติลง `localStorage` |
| `zundo` | ต่อยอดจาก zustand เพื่อเก็บประวัติการแก้ไขตาราง ทำ Undo/Redo (เฉพาะเนื้อหาตาราง ไม่รวม selection หรือ UI ชั่วคราว) |
| `exceljs` | อ่านไฟล์ `.xlsx` ที่ผู้ใช้อัปโหลด และสร้างไฟล์ `.xlsx` ตอนส่งออก (คงสูตรเดิมไว้ในไฟล์ที่ export) |
| `jspdf` + `jspdf-autotable` | สร้างไฟล์ PDF จากตารางที่คำนวณค่าแล้ว |
| `@anthropic-ai/sdk` | เชื่อมต่อ Claude API สำหรับผู้ช่วย AI แนะนำสูตร |
| `lucide-react` | ไอคอนในหน้า UI |
| `clsx` | รวม className แบบมีเงื่อนไข |

> **หมายเหตุ:** ไม่ได้ใช้ไลบรารีคำนวณสูตรสำเร็จรูป (เช่น HyperFormula) แต่เขียน **เอนจินคำนวณสูตรขึ้นเอง**
> (`src/lib/formulaEngine/`) ทั้ง tokenizer, parser, evaluator และฟังก์ชันต่างๆ เพื่อควบคุมพฤติกรรม
> เช่นการอ้างอิงเซลล์ (`A1`, `A1:B10`), การตรวจ circular reference, และการเลื่อนสูตรแบบ relative/absolute
> เวลา "ใส่สูตรให้ทั้งแถว/คอลัมน์" (เหมือนการลาก fill handle ใน Excel) ได้เอง รวมถึงเลี่ยงปัญหาลิขสิทธิ์/ช่องโหว่
> ความปลอดภัยของไลบรารีสำเร็จรูปบางตัว

### เครื่องมือ (Tools)
- **Node.js 18.18+** (แนะนำ Node 20 หรือ 22) และ **npm**
- **ESLint 9** (`eslint-config-next`) สำหรับตรวจโค้ด
- **Git** สำหรับ clone/push โค้ด
- ไม่ต้องใช้ฐานข้อมูลหรือ backend แยก — ทุกอย่างรันอยู่ใน Next.js เดียว (ข้อมูลตารางบันทึกอัตโนมัติลง `localStorage`
  ของเบราว์เซอร์ผู้ใช้ ยังไม่มีระบบบันทึกลงคลาวด์หรือ sync ข้ามเครื่อง)

---

## วิธีดึงไปใช้งาน

### 1) รันในเครื่องตัวเอง (Local Development)

```bash
# 1. โคลนโปรเจกต์
git clone https://github.com/SuruchBoss/ExcelToGo.git
cd ExcelToGo

# 2. ติดตั้ง dependencies
npm install

# 3. รันเซิร์ฟเวอร์สำหรับพัฒนา
npm run dev
```

เปิดเบราว์เซอร์ไปที่ **http://localhost:3000** จะเห็นตารางตัวอย่างพร้อมใช้งานทันที ไม่ต้องตั้งค่าอะไรเพิ่ม

คำสั่งอื่นๆ ที่มีให้:

| คำสั่ง | ใช้ทำอะไร |
|---|---|
| `npm run dev` | รันโหมดพัฒนา (hot reload) |
| `npm run build` | build เป็นเวอร์ชัน production |
| `npm run start` | รันเวอร์ชันที่ build แล้ว (ต้อง `npm run build` ก่อน) |
| `npm run lint` | ตรวจสอบคุณภาพโค้ดด้วย ESLint |

### 2) ตั้งค่าผู้ช่วย AI ให้ใช้ Claude จริง (ไม่บังคับ)

ค่าเริ่มต้น ผู้ช่วย AI จะแนะนำสูตรจาก **การจับคำสำคัญในเครื่อง** (ทำงานได้ทันทีโดยไม่ต้องตั้งค่าอะไร)
แต่ถ้าต้องการให้ AI ตีความคำถามภาษาธรรมชาติได้แม่นยำและยืดหยุ่นกว่าเดิม ให้เชื่อมต่อกับ Claude API:

```bash
# คัดลอกไฟล์ตัวอย่าง
cp .env.example .env.local
```

แล้วเปิด `.env.local` ใส่ค่า:

```
ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxxxxxxxxxx
```

(ขอ API key ได้ที่ [console.anthropic.com](https://console.anthropic.com)) จากนั้นรัน `npm run dev` ใหม่
ระบบจะสลับไปใช้ Claude โดยอัตโนมัติเมื่อเจอค่านี้ — ไม่ต้องแก้โค้ดใดๆ

### 3) Deploy ขึ้นเว็บจริง

โปรเจกต์นี้เป็น Next.js มาตรฐาน จึง deploy ได้กับทุกแพลตฟอร์มที่รองรับ Next.js เช่น:

- **[Vercel](https://vercel.com)** (แนะนำ ง่ายสุด): เชื่อม repo GitHub นี้เข้ากับ Vercel แล้วกด Deploy ได้เลย
  ไม่ต้องตั้งค่าอะไรเพิ่ม ถ้าต้องการ AI จริงให้ไปเพิ่ม Environment Variable ชื่อ `ANTHROPIC_API_KEY`
  ในหน้า Project Settings → Environment Variables
- Self-host ด้วย Docker/Node server ทั่วไป: `npm run build` แล้วรันด้วย `npm run start` (ต้องตั้งค่า `ANTHROPIC_API_KEY`
  เป็น environment variable ของเซิร์ฟเวอร์ถ้าต้องการ AI จริง)

---

## ฟีเจอร์และวิธีใช้อย่างละเอียด

### 1. ตารางกรอกข้อมูล (Spreadsheet Grid)

- คลิกเซลล์เพื่อเลือก, **ดับเบิลคลิก** หรือกด **F2** เพื่อแก้ไขค่าในเซลล์
- พิมพ์ตัวอักษร/ตัวเลขขณะเลือกเซลล์ได้เลยโดยไม่ต้องดับเบิลคลิกก่อน (จะเริ่มพิมพ์ทับค่าเดิมทันที)
- **แถบสูตร (formula bar)** ด้านบนตาราง แสดงที่อยู่และเนื้อหาดิบ (ค่า/สูตร) ของเซลล์ที่เลือกอยู่ตลอดเวลา
  แก้ไขที่แถบนี้ได้เลยโดยไม่ต้องดับเบิลคลิกเข้าไปในเซลล์ กด Enter เพื่อยืนยันแล้วเลื่อนลงแถวถัดไป หรือ Escape เพื่อยกเลิก
- ใช้ปุ่มลูกศร (arrow keys), **Enter** (ลงแถวถัดไป), **Tab** (ไปคอลัมน์ถัดไป) เพื่อเลื่อนเซลล์ที่เลือก
- กด **Delete/Backspace** เพื่อลบค่าของเซลล์หรือทั้งช่วงที่เลือก (คงรูปแบบ/สีไว้ ลบเฉพาะเนื้อหา)
- **เลือกทั้งช่วง** ได้ด้วยการลากเมาส์ หรือกด **Shift** ค้างแล้วคลิกเซลล์ปลายทาง หรือคลิกที่หัวแถว/หัวคอลัมน์เพื่อเลือกทั้งแถว/คอลัมน์
- **หัวแถว (ตัวเลข) และหัวคอลัมน์ (A, B, C, ...) จะถูกปักหมุด (sticky)** ไว้เสมอเวลาเลื่อนตารางที่มีข้อมูลเยอะๆ
  และ **ไฮไลต์สีฟ้า** ให้เห็นชัดว่ากำลังอยู่แถว/คอลัมน์ไหน — ออกแบบมาเพื่อแก้ปัญหา "เลื่อนจอแล้วงงว่าอยู่แถว/คอลัมน์ไหน"
  ที่มักเจอใน Excel ตารางใหญ่ๆ

### 2. บันทึกอัตโนมัติ และ Undo/Redo

- ทุกการแก้ไขตาราง (พิมพ์ค่า, ใส่สูตร, นำเข้าไฟล์) จะถูก **บันทึกอัตโนมัติลงเบราว์เซอร์ปัจจุบัน** ทันที
  (มีข้อความ "บันทึกอัตโนมัติ" อยู่มุมขวาบนของ toolbar ตลอดเวลาเพื่อยืนยัน) — ปิดแท็บหรือรีเฟรชหน้าแล้วข้อมูลจะยังอยู่
  ⚠️ การบันทึกนี้ผูกกับเบราว์เซอร์/เครื่องนั้นๆ เท่านั้น (ไม่ sync ข้ามเครื่อง/อุปกรณ์) หากต้องการเก็บถาวรหรือย้ายเครื่อง
  ให้กด **"ส่งออก Excel"** เพื่อได้ไฟล์ไว้เสมอ
- กดปุ่ม **↶ (เลิกทำ)** / **↷ (ทำซ้ำ)** บน toolbar หรือใช้คีย์ลัด **Ctrl+Z** (เลิกทำ) และ **Ctrl+Y** หรือ
  **Ctrl+Shift+Z** (ทำซ้ำ) เพื่อย้อน/ทำซ้ำการแก้ไขเนื้อหาตาราง — ปุ่มจะจางลงเมื่อไม่มีประวัติให้ย้อน/ทำซ้ำแล้ว
  (การเลื่อน selection หรือเปิด/ปิดแผงสูตรไม่นับเป็นประวัติที่ต้อง undo)

### 3. คัดลอก/ตัด/วาง (Copy, Cut, Paste)

- **Ctrl+C** คัดลอก, **Ctrl+X** ตัด, **Ctrl+V** วางเซลล์หรือทั้งช่วงที่เลือกอยู่ — ช่วง/เซลล์ที่คัดลอกหรือตัดไว้
  จะมีเส้นประไฮไลต์ (สีฟ้า = คัดลอก, สีส้ม = ตัด) ให้เห็นชัดว่ากำลังจะวางอะไร กด **Escape** เพื่อยกเลิก
- วางสูตรที่คัดลอกมา ระบบจะ**ปรับการอ้างอิงเซลล์แบบ relative ให้อัตโนมัติ**เหมือน Excel (เช่นคัดลอก `=A1*B1`
  ไปวางอีกแถวจะกลายเป็น `=A2*B2`) ส่วนการตัด (Cut) จะย้ายเนื้อหาไปจริง (ลบต้นทางหลังวางสำเร็จ)
- วางข้อความจากที่อื่นได้ด้วย (เช่นคัดลอกตารางจาก Excel จริงหรือ Google Sheets มาวาง) โดยจะแยกคอลัมน์/แถวตาม
  Tab/บรรทัดใหม่ให้อัตโนมัติ ระบบจะขยายตารางเพิ่มแถว/คอลัมน์ให้เองถ้าข้อมูลที่วางมีขนาดใหญ่กว่าตารางปัจจุบัน

### 4. จัดรูปแบบเซลล์พื้นฐาน

แถบ **"จัดรูปแบบ"** ใต้ toolbar ใช้จัดรูปแบบเซลล์หรือช่วงที่เลือกอยู่:

- **ตัวหนา (B)** — สลับเปิด/ปิดตัวหนาให้ทั้งช่วงที่เลือก
- **จัดตำแหน่งข้อความ** — ซ้าย / กลาง / ขวา
- **สีข้อความ** — เลือกสีได้อิสระผ่านตัวเลือกสี
- **รูปแบบตัวเลข** — ทั่วไป, ทศนิยม 2 ตำแหน่ง, เปอร์เซ็นต์ (%), หรือสกุลเงิน (฿) — เปลี่ยนเฉพาะการแสดงผล
  ไม่กระทบค่าจริงหรือสูตรที่เก็บไว้ในเซลล์
- การจัดรูปแบบจะติดไปกับเซลล์เวลาคัดลอก/วาง และคงอยู่เวลาส่งออกเป็นไฟล์ Excel ด้วย

### 5. แถวและคอลัมน์: เพิ่ม แทรก ลบ

- ปุ่ม **"+ แถว"** และ **"+ คอลัมน์"** บน toolbar เพิ่มแถว/คอลัมน์ต่อท้ายตาราง
- **คลิกขวา** ที่หัวแถวหรือหัวคอลัมน์ เพื่อ **แทรก** แถว/คอลัมน์ใหม่ก่อนตำแหน่งนั้น หรือ **ลบ** แถว/คอลัมน์นั้นทิ้ง
  (คลิกซ้ายที่หัวแถว/หัวคอลัมน์เพื่อเลือกทั้งแถว/คอลัมน์ก่อนก็ได้)
- เมื่อแทรกหรือลบ ระบบจะ**ปรับสูตรทุกเซลล์ในตารางให้อ้างอิงตำแหน่งใหม่ที่ถูกต้องอัตโนมัติ** — สูตรที่อ้างอิงถึงแถว/คอลัมน์
  ที่ถูกลบไปพอดีจะแสดง `#REF!` ให้เห็นชัดว่าอ้างอิงเสียไปแล้ว เหมือนพฤติกรรมของ Excel

### 6. เรียงลำดับและกรองข้อมูล

- **เรียงลำดับ**: เลือกเซลล์ในคอลัมน์ที่ต้องการ (หรือเลือกทั้งช่วงที่ต้องการเรียง) แล้วกดปุ่ม **↓A-Z** (น้อยไปมาก)
  หรือ **↓Z-A** (มากไปน้อย) บนแถบจัดรูปแบบ — ถ้าเลือกไว้แค่เซลล์เดียว ระบบจะเดาขอบเขตตารางให้อัตโนมัติ
  (ขยายไปจนเจอแถวว่าง) และข้ามแถวหัวตารางให้ถ้าตรวจพบว่าเป็นข้อความอยู่เหนือข้อมูลตัวเลข
  ⚠️ การเรียงจะย้ายเฉพาะเนื้อหาของคอลัมน์ที่เลือกไว้ (เหมือน Excel) และไม่ปรับสูตรให้ตามแถวที่ย้ายไป
  ควรใช้กับคอลัมน์ที่เป็นค่า/ข้อความล้วนมากกว่าคอลัมน์ที่มีสูตรอ้างอิงเซลล์อื่นในตารางเดียวกัน
- **กรองข้อมูล**: กดไอคอนกรวย (🔽) ที่หัวคอลัมน์ เพื่อเลือกว่าจะแสดงแถวที่มีค่าใดบ้างในคอลัมน์นั้น (ติ๊กออกเพื่อซ่อนแถว)
  แถวที่ถูกกรองออกจะไม่แสดงในตาราง แต่หมายเลขแถวที่เหลือยังเป็นเลขจริงเหมือนเดิม (ไม่เรียงใหม่) กด "ล้างตัวกรอง"
  เพื่อแสดงทุกแถวอีกครั้ง

### 7. หลายชีตในไฟล์เดียว (Sheet Tabs)

- แถบชีตด้านล่างตารางใช้ **สลับ / เพิ่ม / เปลี่ยนชื่อ / ลบ** ชีตได้ — กด **"+"** เพื่อเพิ่มชีตใหม่, **ดับเบิลคลิก**
  ชื่อชีตเพื่อเปลี่ยนชื่อ, หรือกดไอคอน **×** (แสดงเมื่อเอาเมาส์ชี้) เพื่อลบ (มีกล่องยืนยันก่อนลบเสมอ)
- แต่ละชีตมีข้อมูล สูตร และรูปแบบของตัวเอง แยกจากกันอิสระ แต่ **Undo/Redo และบันทึกอัตโนมัติครอบคลุมทุกชีตพร้อมกัน**
  (การสลับแท็บไปมาไม่ถูกนับเป็นประวัติที่ต้อง undo)

### 8. นำเข้าไฟล์ Excel ที่มีอยู่แล้ว

1. กดปุ่ม **"นำเข้า Excel"** บน toolbar
2. เลือกไฟล์ `.xlsx` (หรือ `.xlsm`, `.xls`) จากเครื่อง
3. ระบบจะอ่านค่าทุกเซลล์ในไฟล์ รวมถึง **สูตรและรูปแบบ (ตัวหนา/สี/การจัดตำแหน่ง) ที่มีอยู่เดิม** เข้ามาแสดงในตารางทันที
   พร้อมคำนวณผลลัพธ์ใหม่ — ถ้าไฟล์มี**หลายชีต** จะนำเข้าเป็นแท็บแยกให้ครบทุกชีต (แทนที่ชีตทั้งหมดที่มีอยู่ในแอปตอนนี้)

### 9. ใส่สูตรแบบลากวาง (ไม่ต้องจำสูตร)

กดปุ่ม **"สูตร"** บน toolbar เพื่อเปิดแถบสูตรด้านขวา ซึ่งมี:

- ช่องค้นหาสูตร และปุ่มกรองตามหมวดหมู่ (คณิตศาสตร์ / สถิติ / ตรรกะ / ข้อความ / วันที่ / ค้นหา)
- การ์ดสูตรแต่ละอัน แสดงชื่อ, syntax, คำอธิบาย และตัวอย่าง

**วิธีใส่สูตรมี 2 แบบ ทำแบบไหนก็ได้:**

- **ลากแล้ววาง**: ลากการ์ดสูตรจากแถบด้านขวา ไปวางบนเซลล์ที่ต้องการในตาราง (เซลล์จะไฮไลต์สีเขียวตอนลากผ่าน)
- **คลิกแล้วเลือก**: เลือกเซลล์ปลายทางในตารางก่อน แล้วคลิกที่การ์ดสูตร

ทั้งสองแบบจะเปิด **แผงกรอกพารามิเตอร์** ขึ้นมาทางขวา ให้กรอกค่าที่สูตรต้องการ (เช่น ช่วงเซลล์, เงื่อนไข)
สำหรับพารามิเตอร์ที่เป็น "เซลล์" หรือ "ช่วงเซลล์" จะมีปุ่มไอคอนเป้าเล็ง (🎯) ข้างช่องกรอก — กดปุ่มนี้แล้ว
**คลิกหรือลากเลือกเซลล์ในตารางได้เลยแทนการพิมพ์เอง** ที่อยู่เซลล์จะถูกเติมให้อัตโนมัติ พร้อมไฮไลต์แถว/คอลัมน์
ที่เลือกให้เห็นชัดเจน (แก้ปัญหาเรื่องงงว่าเลือกช่วงถูกไหม)

![แผงกรอกพารามิเตอร์สูตร SUM กำลังเลือกช่วง C2:C4 จากตาราง พร้อมตัวเลือกใส่สูตรให้ทั้งแถว/คอลัมน์/ช่วงที่เลือก](docs/screenshots/02-formula-panel.png)

ด้านล่างของแผงมีตัวเลือก **"ใส่สูตรนี้ให้กับ"**:
- **เซลล์นี้เท่านั้น** — ใส่ที่เซลล์เดียว
- **ทั้งแถว** — ใส่สูตร (ปรับอ้างอิงให้เหมาะสมอัตโนมัติ) ในทุกคอลัมน์ของแถวนั้น
- **ทั้งคอลัมน์** — ใส่สูตรในทุกแถวของคอลัมน์นั้น (เหมือนลาก fill handle ลงทั้งคอลัมน์ใน Excel)
- **ช่วงที่เลือกไว้** — ใส่สูตรทั่วทั้งช่วงเซลล์ที่เลือกไว้ก่อนหน้า (ใช้ได้เมื่อเลือกมากกว่า 1 เซลล์)

ระบบจะปรับการอ้างอิงเซลล์แบบ relative ให้อัตโนมัติเหมือน Excel (เช่นลาก `=A1*B1` ลงแถวถัดไปจะกลายเป็น `=A2*B2`)
ส่วนการอ้างอิงแบบ absolute (ใส่ `$` เช่น `$A$1`) จะไม่เลื่อนตาม

กดปุ่ม **"ใส่สูตร"** เพื่อยืนยัน หรือ **"ยกเลิก"** เพื่อปิดแผงโดยไม่ใส่ หลังกดใส่สูตร ผลลัพธ์จะคำนวณขึ้นในเซลล์ทันที:

![หลังกดใส่สูตร เซลล์ B6 แสดงผลลัพธ์ SUM(C2:C4) เป็น 100 ทันที](docs/screenshots/03-after-insert.png)

### 10. ถาม AI หาสูตร

กดปุ่ม **"ถาม AI"** บน toolbar เพื่อเปิดแถบผู้ช่วย AI ด้านขวา:

1. พิมพ์สิ่งที่ต้องการเป็นประโยคภาษาไทยหรืออังกฤษ เช่น
   - "อยากรวมยอดขายทั้งหมดในคอลัมน์นี้"
   - "หาค่าเฉลี่ยของคะแนนสอบ"
   - "ถ้าคะแนนมากกว่า 50 ให้ขึ้นว่า ผ่าน"
   - "ค้นหาราคาสินค้าจากรหัสสินค้า"
   (หรือกดปุ่มตัวอย่างคำถามที่มีให้ด้านล่างช่องพิมพ์)
2. กด **"ถาม AI"** (หรือกด Enter) ระบบจะส่งคำถาม พร้อมช่วงเซลล์ที่กำลังเลือกอยู่ ไปให้ AI แนะนำ
3. ผลลัพธ์จะแสดงสูตรที่แนะนำพร้อมคำอธิบายสั้นๆ
4. กด **"ใส่สูตรนี้ที่เซลล์ ..."** เพื่อใส่สูตรลงเซลล์ที่เลือกอยู่ทันที (ใส่ที่เซลล์เดียว — ถ้าต้องการลากทั้งแถว/คอลัมน์
   ให้ไปปรับที่แถบ "สูตร" แทนหลังใส่)

![แถบผู้ช่วย AI แสดงคำแนะนำสูตร =SUM(C2:C4) จากคำถาม "อยากรวมยอดขายทั้งหมดในคอลัมน์นี้"](docs/screenshots/04-ai-assistant.png)

> ถ้ายังไม่ได้ตั้งค่า `ANTHROPIC_API_KEY` ระบบจะแนะนำสูตรจากการจับคำสำคัญเบื้องต้นแทน (มีข้อความแจ้งใต้คำแนะนำเสมอ)
> ยังใช้งานได้ปกติ เพียงแต่แม่นยำน้อยกว่าการต่อ Claude จริง

### 11. ส่งออกไฟล์

- **ส่งออก Excel**: กดปุ่ม "ส่งออก Excel" จะได้ไฟล์ `.xlsx` ไฟล์เดียวที่มี**ทุกชีต**อยู่ครบ (คนละแท็บในไฟล์ Excel)
  พร้อมทั้งค่าที่คำนวณแล้ว **สูตรต้นฉบับ** และ**รูปแบบ (ตัวหนา/สี/การจัดตำแหน่ง/รูปแบบตัวเลข)** ติดไปด้วย
  (เปิดใน Excel/Google Sheets แล้วแก้ไขสูตรต่อได้ปกติ)
- **ส่งออก PDF**: กดปุ่ม "ส่งออก PDF" จะได้ไฟล์ PDF ของ**ชีตที่กำลังเปิดอยู่เท่านั้น** (แสดงเฉพาะค่าที่คำนวณแล้ว
  พร้อมหัวแถว/คอลัมน์ ไม่รวมรูปแบบสี/ตัวหนา) เหมาะสำหรับพิมพ์หรือส่งให้คนอื่นดูโดยไม่ต้องแก้ไข — ถ้าต้องการ PDF
  ของชีตอื่น ให้สลับไปที่ชีตนั้นก่อนแล้วค่อยกดส่งออก

---

## โครงสร้างโปรเจกต์

```
src/
  app/
    page.tsx                 # หน้าเว็บหลัก แค่ประกอบคอมโพเนนต์ตาม state จาก store (ไม่ถือ state เอง)
    api/ai/formula/route.ts  # API endpoint ให้ AI แนะนำสูตร (ใช้ Claude หรือ fallback)
  store/
    sheetStore.ts            # Zustand store — แหล่งความจริงเดียวของ state ทั้งแอป: ชีตทั้งหมด (sheets),
                              # ชีตที่เปิดอยู่ (activeSheetId), selection/ตัวกรองต่อชีต, แผงสูตรที่กำลังกรอก,
                              # แถบข้างที่เปิดอยู่ — และ action ทั้งหมดที่แก้ state นี้ ต่อด้วย middleware
                              # persist (บันทึกอัตโนมัติ) + zundo (undo/redo) ที่ครอบคลุมทุกชีตร่วมกัน
  features/
    grid/SpreadsheetGrid.tsx        # ตารางหลัก (เลือกเซลล์, แก้ไข, sticky header, คลิกขวาแทรก/ลบแถว-คอลัมน์,
                                     # ซ่อนแถวที่ถูกกรอง, drag-drop รับสูตร)
    grid/FormulaBar.tsx             # แถบสูตรด้านบนตาราง แสดง/แก้ไขเนื้อหาดิบของเซลล์ที่เลือก
    grid/SheetTabs.tsx              # แถบแท็บชีตด้านล่างตาราง (สลับ/เพิ่ม/เปลี่ยนชื่อ/ลบชีต)
    grid/ColumnFilterPopover.tsx    # ป็อปอัปเลือกค่าที่จะแสดง/ซ่อนสำหรับตัวกรองแต่ละคอลัมน์
    formulas/FormulaPalette.tsx     # แถบรายการสูตร ค้นหา/กรองหมวดหมู่/ลากวาง
    formulas/FormulaParamPanel.tsx  # แผงกรอกพารามิเตอร์สูตร + เลือกช่วงจากตาราง + เลือก scope
    ai/AIAssistantPanel.tsx         # แชทถาม AI หาสูตร
    toolbar/Toolbar.tsx             # แถบเครื่องมือด้านบน (นำเข้า/ส่งออก/undo-redo/เพิ่มแถว-คอลัมน์/สลับแถบข้าง)
    toolbar/FormatBar.tsx           # แถบจัดรูปแบบเซลล์ + ปุ่มเรียงลำดับข้อมูล
  lib/                       # ตรรกะหลักของโดเมน ไม่ผูกกับ React/UI แก้ไข/ทดสอบแยกจาก UI ได้อิสระ
    formulaEngine/           # เอนจินคำนวณสูตรที่เขียนเอง (tokenizer, parser, evaluator, functions,
                              # การเลื่อนสูตรแบบ relative ตอน fill/paste และแบบโครงสร้างตอนแทรก/ลบแถว-คอลัมน์)
    formulaCatalog.ts        # รายการสูตรสำเร็จรูปที่แสดงในแถบ "สูตร"
    cellFormat.ts            # รูปแบบเซลล์ (ตัวหนา/สี/จัดตำแหน่ง/รูปแบบตัวเลข) + แปลงเป็น/จาก numFmt ของ Excel
    aiHeuristic.ts           # ตรรกะแนะนำสูตรจากคำสำคัญ (ใช้เมื่อไม่มี ANTHROPIC_API_KEY)
    sheet.ts                 # โมเดลข้อมูลตาราง, คำนวณค่าทั้งชีต, ใส่สูตรตาม scope ต่างๆ, คัดลอก/วาง,
                              # แทรก/ลบแถว-คอลัมน์, เรียงลำดับ
    excelIO.ts                # นำเข้า/ส่งออก workbook หลายชีต (.xlsx) ด้วย exceljs พร้อมรูปแบบเซลล์
    pdfExport.ts              # ส่งออก PDF ด้วย jspdf + jspdf-autotable
  types/
    sheet-ui.ts               # types สำหรับ selection ของตารางฝั่ง UI
```

ทุกคอมโพเนนต์ใน `features/` อ่าน/แก้ state ผ่าน `useSheetStore` โดยตรง (ไม่ผ่าน props จาก `page.tsx`)
ทำให้ไม่ต้องส่ง props เป็นทอดๆ (prop drilling) และเพิ่มฟีเจอร์ใหม่ (เช่น undo/redo, บันทึกอัตโนมัติ)
ได้ง่ายในที่เดียวคือ `store/sheetStore.ts`

---

## รายการสูตรที่รองรับ

| หมวดหมู่ | สูตร |
|---|---|
| คณิตศาสตร์ | `SUM`, `PRODUCT`, `ROUND`, `ABS`, `SUMIF` |
| สถิติ | `AVERAGE`, `COUNT`, `COUNTA`, `MIN`, `MAX`, `COUNTIF`, `AVERAGEIF` |
| ตรรกะ | `IF`, `IFERROR`, `AND`, `OR` |
| ข้อความ | `CONCATENATE`, `UPPER`, `LOWER`, `TRIM`, `LEFT`, `RIGHT` |
| วันที่ | `TODAY`, `NOW` |
| ค้นหา | `VLOOKUP` |

เอนจินคำนวณยังรองรับการอ้างอิงเซลล์/ช่วงเซลล์ (`A1`, `A1:B10`), การอ้างอิงแบบ absolute (`$A$1`),
ตัวดำเนินการคำนวณ/เปรียบเทียบ/ต่อข้อความ (`+ - * / ^ = <> < > <= >= &`) และแจ้งค่า error แบบ Excel
เช่น `#DIV/0!`, `#VALUE!`, `#NAME?`, `#N/A`, `#REF!` (อ้างอิงถึงแถว/คอลัมน์ที่ถูกลบไปแล้ว), `#CIRCULAR!`
(กรณีสูตรอ้างอิงกันเป็นวงกลม) — และปรับการอ้างอิงในสูตรให้อัตโนมัติทั้งตอนคัดลอก/ลาก (relative) และตอนแทรก/ลบ
แถวหรือคอลัมน์ (โครงสร้างของตาราง) เหมือน Excel

ต้องการสูตรเพิ่มเติม? เพิ่มได้ที่ `src/lib/formulaEngine/functions.ts` (ตัวเอนจิน) และ `src/lib/formulaCatalog.ts`
(รายการที่แสดงในแถบสูตรพร้อมฟอร์มกรอกพารามิเตอร์)

---

## ปัญหาที่พบบ่อย

**Q: นำเข้าไฟล์ Excel แล้วขึ้น error ว่านำเข้าไม่ได้**
A: ตรวจสอบว่าไฟล์เป็น `.xlsx` ที่ไม่เสียหาย (ไฟล์ `.xls` แบบเก่ามากๆ หรือไฟล์ที่ล็อกรหัสผ่านอาจอ่านไม่ได้)

**Q: ผู้ช่วย AI ตอบแปลกๆ หรือแนะนำสูตรไม่ตรงที่ต้องการ**
A: ถ้ายังไม่ได้ตั้งค่า `ANTHROPIC_API_KEY` ระบบจะแนะนำจากคำสำคัญง่ายๆ เท่านั้น (จะมีข้อความแจ้งเตือนใต้คำตอบเสมอ)
ให้ตั้งค่า API key ตามขั้นตอนใน [หัวข้อที่ 2](#2-ตั้งค่าผู้ช่วย-ai-ให้ใช้-claude-จริง-ไม่บังคับ) เพื่อความแม่นยำที่ดีขึ้น

**Q: ใส่สูตรแล้วเซลล์ขึ้น `#CIRCULAR!`**
A: แปลว่าสูตรในเซลล์นั้นอ้างอิงกลับมาที่ตัวเองทางอ้อม (เช่น A1 อ้างถึง B1 และ B1 อ้างถึง A1) ให้แก้สูตรให้ไม่วนกลับ

**Q: ข้อมูลที่กรอกหายไปเมื่อรีเฟรชหน้าเว็บ**
A: ตารางจะถูกบันทึกอัตโนมัติลง `localStorage` ของเบราว์เซอร์นั้นทุกครั้งที่แก้ไข รีเฟรชหรือปิดแท็บแล้วเปิดใหม่
ข้อมูลจะยังอยู่ตราบใดที่ใช้เบราว์เซอร์/เครื่องเดิมและไม่ได้ล้างข้อมูลเว็บไซต์ (เช่น ล้าง site data หรือเปิดโหมด
ไม่ระบุตัวตน) แต่การบันทึกนี้ไม่ sync ข้ามเครื่อง/เบราว์เซอร์อื่น ถ้าต้องการสำรองถาวรหรือย้ายไปเปิดที่อื่น
ให้กด **"ส่งออก Excel"** เพื่อได้ไฟล์เก็บไว้เสมอ

**Q: กด Ctrl+Z แล้วไม่มีอะไรเกิดขึ้น**
A: ตรวจสอบว่า focus ไม่ได้อยู่ในช่องพิมพ์ข้อความ (เช่น กำลังแก้ไขเซลล์ค้างอยู่ หรือ focus อยู่ในช่องถาม AI) เพราะตอนนั้น
เบราว์เซอร์จะให้ความสำคัญกับ undo ของช่องพิมพ์นั้นก่อน ให้กด Enter/Escape ออกจากการแก้ไขก่อน แล้วลองกด Ctrl+Z อีกครั้ง
หรือกดปุ่ม ↶ บน toolbar แทนก็ได้
